
// Implemet with:

import cdm

def ExtractFullNameWith(name: cdm.HumanName) : String => {
  with (name) {
    concat([concat(prefix, " "),
            ensure(family),
            ensure(given),
            concat(suffix, " ")], " ")
  }
}
